---
title: "Revenue by Channel"
order: 2
section: "revenue"
---

import ChannelChart from '../../components/charts/ChannelChart';
import layers from '../../data/layers.json';
export const revenueLayer = layers[0];
export const retailers = revenueLayer.channels.filter(c => c.channel_type === 'retailer');
export const distributors = revenueLayer.channels.filter(c => c.channel_type === 'distributor');
export const dtc = revenueLayer.channels.filter(c => c.channel_type === 'DTC');

## {revenueLayer.label}

<p class="section-subtitle">{revenueLayer.subtitle}</p>

Revenue concentration is the first thing to notice. Walmart alone accounts for 44% of total revenue — a dependency that makes every Walmart-specific deduction a material event. But comparing Walmart to UNFI is misleading: one is a retailer, the other a distributor. The business relationship, cost structure, and deduction profile are fundamentally different. What follows separates channels by segment so like is measured against like.

<p class="segment-label">Retailers</p>

<ChannelChart
  client:visible
  data={retailers}
  layerLabel="Gross Revenue — Retailers"
  valueLabel="gross revenue"
/>

<p class="segment-label">Distributors</p>

<ChannelChart
  client:visible
  data={distributors}
  layerLabel="Gross Revenue — Distributors"
  valueLabel="gross revenue"
/>

<p class="segment-label">Direct-to-Consumer</p>

<ChannelChart
  client:visible
  data={dtc}
  layerLabel="Gross Revenue — DTC"
  valueLabel="gross revenue"
/>

<p class="chart-footnote">Source: Cinderhaven orders data, all channels, year to March 2026. Revenue is gross invoiced amount before any deductions or adjustments.</p>
