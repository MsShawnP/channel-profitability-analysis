---
title: "Trade Deductions"
order: 3
section: "deductions"
---

import ChannelChart from '../../components/charts/ChannelChart';
import layers from '../../data/layers.json';
import channels from '../../data/channels.json';

export const grossMarginLayer = layers[1];
export const deductionsLayer = layers[2];
export const walmartDeductions = channels.find(c => c.channel_id === 'walmart');
export const costcoChannel = channels.find(c => c.channel_id === 'costco');

## {deductionsLayer.label}

<p class="section-subtitle">{deductionsLayer.subtitle}</p>

Trade deductions are the first invisible tax. They never appear on the income statement as a line item — they arrive as post-invoice adjustments, chargebacks, and remittance shortfalls. Across all channels, Cinderhaven surrendered $1.5M to trade deductions last year.

Walmart's $655K in deductions is the largest absolute number, but that reflects its revenue dominance. The more telling metric is deduction intensity: Costco loses 8.5% of revenue to deductions — nearly double Walmart's 4.7% rate — on a fraction of the volume. Short ships and promo billbacks account for the majority at every channel; unclassified deductions (labelled "vague" in remittance data) are the third-largest category, suggesting a documentation gap worth closing.

<ChannelChart
  client:visible
  data={deductionsLayer.channels}
  layerLabel="Revenue After Trade Deductions"
  valueLabel="post-deduction revenue"
/>

<p class="chart-footnote">Source: Cinderhaven deductions ledger matched to orders. Trade deductions include short ships, promotional billbacks, slotting fees, and unclassified chargebacks. Click any bar to see the breakdown by deduction type.</p>
